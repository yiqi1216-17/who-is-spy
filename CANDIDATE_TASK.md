# 面试任务书 · AI 谁是卧底(候选人版)

欢迎。这是一道**带回家 + 现场讲解**的 Agent 全栈工程题。你会拿到一个**已经能跑通完整
对局**的「AI 谁是卧底」项目:一名人类玩家与四名独立的 DeepSeek Agent 同桌推理,服务端
负责隐藏信息、规则校验和胜负裁决。

你的任务**不是从零搭建**,而是在这个真实基线上做**二次开发**,把它从"多个隔离的 LLM
玩家调用"推进成一个**有差异化策略、可评测、可观测、可演进**的 Multi-Agent 系统。

> 我们更看重你**如何定位问题、如何做工程取舍、如何落地并自证**,而不是页面数量或代码量。
> 请把 Coding Agent(Cursor / Claude Code / TraeCode 等)当作日常工具正常使用,但你必须
> 能对交付的每一行关键代码负责并现场解释。

---

## 0. 仓库结构与技术栈选择

这是一个 monorepo。**前端一份**,后端**两份等价实现**(Node 与 Go),它们对外暴露**完全
相同的 HTTP 契约**:

```text
packages/
  web/           React 前端(所有后端共用)
  server-node/   Node/TypeScript 后端实现
  server-go/     Go 后端实现(与 Node 行为等价)
contract/        语言无关的黑盒契约测试(见下)
```

**你只需二选一**:用 Node **或** Go 完成后端部分,不要求两个都改。前端是**可选加分层**,
不是必做项(见第 3 节)。

契约测试 `contract/` 是一份语言无关的黑盒校验,它冻结了**所有实现都必须满足的基线硬
门槛**(HTTP 形状、信息隔离、确定性裁决、模型调用接口)。它**不**规定你二次开发的内部
做法。无论你选哪个栈、怎么改,契约必须**持续全绿**。契约运行时使用 FakeModel,因此真实
模型路径还需要你自行验证,并在现场演示:

```bash
npm run contract:node   # 校验 Node 后端
npm run contract:go     # 校验 Go 后端
```

契约冻结了什么、留了哪些发挥空间,详见 [`contract/CONTRACT.md`](./contract/CONTRACT.md)。

---

## 1. 环境与基线

要求 Node.js 20.19+ / 22.12+ / 24+(推荐 Node 22 LTS;依赖不支持 Node 21/23);若选 Go,
需 Go 1.22+(项目在 1.26 验证)。

```bash
npm install
cp .env.example .env        # 填入可用的 DeepSeek / OpenAI-compatible Key

# —— 选 Node ——
npm run dev:node            # 后端 :8787
npm run dev:web             # 前端 :5173(代理 /api 到 8787)
npm run test:node           # 基线域测试应全绿
npm run contract:node       # 语言无关契约应全绿

# —— 选 Go ——
npm run dev:go              # 后端 :8787
npm run dev:web             # 前端 :5173
npm run contract:go         # 语言无关契约应全绿
```

**动手前请先通读并跑通基线**。下列事实是评分硬门槛,不允许被你的改动破坏。契约会持续
校验 HTTP、信息隔离、裁决和模型接口;真实模型是否可用以现场运行结果为准:

- AI 的描述、投票、复盘由**真实模型**生成,不是预设文本或随机模板。
- 任一 Agent 决策时**读不到**其他玩家的秘密词或身份。
- 淘汰、同票加赛、胜负、合法投票目标由**确定性代码**裁决。
- 服务端是唯一事实源;前端只发意图。
- 终局前公开 DTO 不泄露其他玩家的 `role / word`。

阅读顺序建议(以 Node 为例,Go 同名文件一一对应):
`types` → `agent-context`(信息隔离)→ `game-engine`(权威状态机)→ `model`(模型客户端与
结构化校验)→ `app`(HTTP 与错误映射)。测试替身在 `test-utils` / `fake_model`。

---

## 2. 带回家任务

三条带回家任务线,外加一条**现场环节**。**任务线①必做**;**②③自选深度**(至少完整
完成其一,另一条可只做核心);**现场环节**在第 4 节说明,带回家**无需预先准备**。

每条都给出**你会观察到的现象**,不直接给解法——**如何定位与设计是评分项之一**。请用测试
或可运行脚本自证,而不是只在文档里声明。

### 任务线① · Agent 决策与编排(必做)

先自己玩几局或看批量样本,你会注意到:

- 四个 AI 的描述**风格高度雷同**,像同一个人说了四遍;有时会**过度具体**,几乎指向答案。
- 同一轮里,后发言的 AI **并不知道**同轮先发言的 AI 说了什么。
- 每个角色在代码里带了一个"人设"字段(谨慎观察 / 直觉敏锐 / 逻辑派 / 出其不意),但你
  会发现它**对 AI 的实际行为没有任何影响**。

请让这些 Agent 真正像**四个有各自策略、且会利用逐步公开信息**的玩家。

> 验收信号:提供测试或脚本,证明(a)后发 Agent 的上下文里确实包含**同轮先发**的公开
> 描述;(b)四个角色在**相同局面**下产出**可区分**的行为(表达/投票倾向);(c)一条
> "雷同或泄题"的描述会被你的机制**拦下**(重试或降级),而不是直接推进对局。
> 全程不得打破信息隔离边界。

### 任务线② · 效果评测(自选深度)

基线只有少量功能测试和一次手工整局记录,**没有任何质量指标**。你无法回答"这一版比上一版
更好还是更差"。

请建立**可复现的批量对局评测**,让 Agent 行为质量变得**可量化、可回归**。

> 验收信号:一个命令能跑 N 局并输出指标表(至少覆盖泄题、描述同质化、有效投票、完局、
> 延迟、成本这几类);有一组**固定/可复现**的回归用例;并且至少有一个**门禁**——当质量
> 指标越过阈值时评测**非 0 退出**。提示:引擎的随机源已可注入,便于构造确定性局面。

### 任务线③ · 可观测性与故障恢复(自选深度)

基线有超时、重试、结构化校验和错误映射,但一次失败你**只会看到一句"模型出错"**——无法
知道是哪一局、哪一轮、哪个 AI、第几次重试出的问题,也没法复现。

请让这个系统在出问题时**能被看见、能被复现、能优雅降级**。

> 验收信号:能人为**制造故障**(如超时 / 返回坏 JSON / 触发限流 / 个别 AI 失败)来演示
> 降级怎么走;出问题时能**精确定位到"哪一局、哪一轮、哪个 AI、第几次重试、什么错误"**
> (且日志里不出现明文 Key 或完整密词);并能把一局的关键决策**回放**出来,方便复盘。
> 个别 AI 失败时,这一局要么安全继续、要么明确中止,**不能留下一个做了一半的状态**。

### 任务线④ · 现场环节:架构演进(带回家无需准备)

现场会**当场揭晓**一个此前未公开的**架构变体需求**,请你用**日常的 Coding Agent**在
约 15 分钟内**现场落地并回归**。带回家阶段**无需为它做任何准备**——我们要看的正是你面对
一个**没有预案的变更**时,如何拆解、如何驾驭 Coding Agent、如何把新规则收敛到清晰的边界
而不散落地改条件分支,同时不破坏信息隔离与终局揭示。

> 现场请自带**可用的 Coding Agent 环境**与**能真实跑通的项目**(见第 3 节环境要求)。

---

## 3. 交付物与约束

1. **保持基线硬门槛不破坏**:你所选栈的契约(`contract:node` 或 `contract:go`)必须
   持续全绿;真实模型路径可用。请为你的新功能补充测试。
2. **前端与工程优化是可选加分层**(不顶替①②③,也不能拿来替代②③的"自选深度"):
   - **前端加分**:核心任务在后端。若你的功能(如评测面板、trace 视图、故障注入开关)在
     前端有自然的呈现,做了会加分;也可优化观战 / 投票 / 复盘的**信息呈现与交互**,让它更
     贴合"谁是卧底"的推理体验(如描述时间线、投票可视化、终局揭示)。不做**不扣**核心分。
   - **后端工程优化加分**:围绕**工程健壮性与业务理解**做锚定优化,如状态持久化 / 请求
     幂等(现为纯内存)、配置化、结构化分层与可测性——但**不得破坏契约与信息隔离**。
3. **过程证据 `DECISIONS.md`**(直接对应"Coding Agent 使用"维度):
   - 你用 Coding Agent 做了哪些改动、在哪里做了人工审查和修正;
   - 至少一处 Agent 给出的**错误/次优建议**以及你如何纠正;
   - 关键设计取舍(为什么这样做、放弃了什么);
   - 你选择了 Node 还是 Go,以及为什么。
4. **不要提交密钥**:`.env` 已被忽略;trace/回放产物中不得包含明文 Key 或完整密词。
5. **提交历史**:请**分多次有意义地提交**(基线尚无 commit,你的第一提交可作为
   "baseline import"),现场需能**投屏展示你的 git log**,让我们看到你的推进过程。
6. **保障运行环境稳定**:现场需**真实演示**,请确保带回家的项目能在现场**稳定跑通**
   (真实模型可用、依赖就绪、Coding Agent 环境可用)。环境跑不起来由你负责。
7. 允许调整依赖,但需说明理由;不引入与题目无关的重型框架来"堆能力"。

时间预期:核心任务线大约 6–10 小时投入;不追求全做满,但**做到的部分要有证据**。做不完
的部分,请在 `DECISIONS.md` 里诚实标注取舍。

---

## 4. 现场环节(约 60–90 分钟)

带 `DECISIONS.md`、**可用的 Coding Agent 环境**和**能真实跑通的项目**来。现场包含:

1. **代码讲解**:随机抽取你交付的核心逻辑,请你逐段解释——Agent 上下文构造、状态流转、
   你新增的评测/trace 逻辑。"测试通过"不能替代对代码的理解。
2. **真实运行**:现场用真实模型跑一局,并展示你的评测 / trace / 故障注入;我们可能请你
   **当场修改一个输入(如换词或改随机种子)并重跑**,或**当场触发一次真实模型调用查看原始
   返回**。请确保环境稳定。
3. **任务线④ · 现场架构变体(当场揭晓)**:我们会给你一个**此前未公开**的架构变更需求,
   请你用 Coding Agent 在约 15 分钟内**现场落地并回归**——重点看你能否把新规则收敛在
   **清晰的策略边界**内,而不是散落地改条件分支,且不破坏信息隔离与终局揭示。

---

## 5. 评分

评分以**运行结果 + 代码 + 验证证据**三者交叉确认:只有声明、只有类名、只有一次成功
对局,都不构成得分。与本任务书直接相关的重点是:Agent 独立决策与编排、AI 行为质量与
游戏性、调试可观测性与故障恢复、评测与回归保护、Coding Agent 使用与人工验收、现场变体
与架构迁移。
